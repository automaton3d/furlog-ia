<?php
/**
 * Copie este arquivo para db.php e preencha com as credenciais reais.
 * db.php NÃO deve ser versionado (está no .gitignore).
 */
$conn = new mysqli("localhost", "USUARIO", "SENHA", "gedcom");
if ($conn->connect_error) {
    die("Erro na conexão: " . $conn->connect_error);
}
$conn->set_charset("utf8mb4");
?>
